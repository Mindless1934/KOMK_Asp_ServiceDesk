@model IEnumerable<jSkillList>
<div class="trWe2">
    @{
        ViewBag.Title = "Index";
    }

    <h2>Распределение умений</h2>

    <p>
        @Html.ActionLink("Добавить", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                Умение
            </th>
            <th>
                Пользователь
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.hSkill.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AspNetUsers.Fio)
                </td>
                <td>
                    <div class="postr">
                        @Html.ActionLink("Изменить", "Edit", new { id = item.SkillListId }, new { @class = "btn btn-default" })
                        @Html.ActionLink("Удалить", "Delete", new { id = item.SkillListId }, new { @class = "btn btn-default" })
                    </div>
                </td>
            </tr>
        }

    </table>
</div>